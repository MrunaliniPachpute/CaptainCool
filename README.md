# CaptainCool AI

A multi-agent IPL strategist system where multiple Gemini-powered agents debate tactical cricket decisions.

## Setup
1. Copy `.env.example` to `.env` and add your Gemini API key.
2. Run `npm install`.
3. Run `npm start` (or `node server.js`).
4. Open `http://localhost:3000` in your browser.
